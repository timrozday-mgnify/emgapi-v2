# Generated by Django 5.1.6 on 2025-04-09 11:45
from typing import TYPE_CHECKING

from django.db import migrations, models

if TYPE_CHECKING:
    from analyses import models as mg_models


def set_external_results_dirs(apps, schema_editor):
    Study: "mg_models.Study" = apps.get_model("analyses", "Study")

    for study in Study.objects.filter(results_dir__isnull=False).exclude(
        results_dir__startswith="/"
    ):
        study.external_results_dir = study.results_dir
        print(
            f"Study {study} has external results dir {study.external_results_dir} now"
        )
        study.save()

    Analysis: "mg_models.Analysis" = apps.get_model("analyses", "Analysis")

    for analysis in Analysis.objects.filter(results_dir__isnull=False).exclude(
        results_dir__startswith="/"
    ):
        analysis.external_results_dir = analysis.results_dir
        print(
            f"Analysis {analysis} has external results dir {analysis.external_results_dir} now"
        )
        analysis.save()


class Migration(migrations.Migration):

    dependencies = [
        ("analyses", "0040_remove_study_has_legacy_data_study_features_and_more"),
    ]

    operations = [
        migrations.AddField(
            model_name="analysis",
            name="external_results_dir",
            field=models.CharField(blank=True, max_length=256, null=True),
        ),
        migrations.AddField(
            model_name="study",
            name="external_results_dir",
            field=models.CharField(blank=True, max_length=256, null=True),
        ),
        migrations.RunPython(
            code=set_external_results_dirs,
            reverse_code=migrations.RunPython.noop,
        ),
    ]
